export enum ENUM_NOTIFICATION_STATUS {
    SEEN = "seen",
    UNSEEN = "unseen",
  }
  
  export enum ENUM_NOTIFICATION_TYPE {
    PRIVATEMESSAGE = "privateMessage",
    OFFER="offer",
   ORDER="order",
   DELIVERY="delivery",
   REVISION="revision"
  }